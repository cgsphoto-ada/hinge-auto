# Secrets
.env

# Calibrated coordinate files (per-device, never commit real values)
filter_coords.json
location_coords.json
matches_coords.json
locations.json

# Calibration captures from interactive coord mapping
_cal/

# Debug + screenshots — match data, decisions, captured frames
debug/
screenshots/

# Captured screenshots and ad-hoc state files
_state.png
_*.png
*.png
calibrate.png

# ...but README brand art is intentional, not capture data — keep it tracked
!docs/assets/*.png

# Runtime logs
cron.log

# Personal mode + voice configs (not shared)
modes/carlos.py
voice/carlos.py
voice/carlos_no_msg.py

# Test scripts
test_webhook.py

# Python
__pycache__/
*.pyc
*.pyo
.venv/
venv/
.python-version

# Editor / OS
.vscode/
.idea/
.DS_Store
Thumbs.db
